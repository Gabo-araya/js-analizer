# Variables de entorno para NTG JS/CSS Analyzer
# Copia este archivo a .env y configura los valores apropiados

# === Configuración de Flask ===
FLASK_ENV=production
FLASK_DEBUG=0
FLASK_SECRET_KEY=tu-clave-secreta-muy-larga-y-segura-aqui

# === Configuración de la aplicación ===
# Puerto en el que se ejecutará la aplicación (dentro del contenedor)
FLASK_PORT=5000

# Zona horaria
TZ=America/Santiago

# === Configuración de base de datos ===
# La base de datos se almacenará en el volumen montado
DB_PATH=/app/data/analysis.db

# === Configuración de red ===
# Puerto expuesto en el host (cambiar si hay conflictos)
HOST_PORT=5000

# === Configuración de recursos ===
# Límites de memoria y CPU (opcional)
MEMORY_LIMIT=1GB
CPU_LIMIT=0.5
MEMORY_RESERVATION=256MB
CPU_RESERVATION=0.1

# === Configuración de logging ===
LOG_LEVEL=INFO
LOG_MAX_SIZE=10m
LOG_MAX_FILES=3

# === Configuración de seguridad ===
# Habilitar/deshabilitar características de seguridad
SECURITY_HEADERS=true
CSRF_PROTECTION=true

# === Configuración de análisis ===
# Timeout para requests HTTP (en segundos)
HTTP_TIMEOUT=30
# Límite de archivos a analizar por sitio
MAX_FILES_PER_SITE=10
# Delay entre requests en análisis masivo (en segundos)
BATCH_DELAY=2